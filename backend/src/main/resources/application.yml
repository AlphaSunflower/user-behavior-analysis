server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_behavior_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.group18.model
  configuration:
    map-underscore-to-camel-case: true

# Kafka 配置（黑名单指令）
kafka:
  bootstrap-servers: 192.168.100.140:9092
  blacklist-topic: user_blacklist
